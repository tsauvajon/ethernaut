There is a type in the constructor: `Fal1out` instead of `Fallout`.
Therefore, the constructor was never called on contract deployment
and you can simply call `contract.Fal1out()` to get the contract
ownership.